/**
 * Availability pill (brief §5.1). Hidden when `enabled` is false.
 * The text must stay truthful: do not add a slot count without a real number.
 */
export const availability = {
  enabled: true,
  label: 'Menerima proyek baru',
} as const;
